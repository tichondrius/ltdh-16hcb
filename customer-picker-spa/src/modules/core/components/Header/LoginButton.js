import React from 'react';
import FlatButton from 'material-ui/FlatButton';

const LoginButton = (props) => (
  <FlatButton style={{color: '#fff'}} {...props} label="Login" />
)

export default LoginButton;